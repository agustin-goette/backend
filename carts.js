/*import { log } from "console";
import express from "express"
import fs from "fs"
import { json } from "stream/consumers";

const app = express();
const port= 8080 ;


app.get("/carts",(request,response)=>{
    let contenidoHTML = "<h1>Hola mundo</h1>"
   response.end(contenidoHTML);
})

app.listen(port, () =>{
   

})*/